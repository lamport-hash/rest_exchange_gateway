these rules should always be followed:

for any architectural decision consult the user
for any core objects changes or logic changes consult the user
every function should be tested with a test illustrating the normal use case
for every function dangerous edge cases should be listed, and tested if behaviour is not trivial
